#skip

.comment "I'M ALIIIIVE












as








"  ;test

.name "asd


"
#asd
		


l2:

l3:sti r1,%:live, %1 	 ;7 bytes
		and r1, %0, r1 		# 8 bytes

live:	live %1				# 5 bytes
		zjmp %:live			# 3 bytes

# header_get_cmd()
	# skip all comments and end lines
	# if .(char) found then find first (") char
		# cut line from .name/comment until last quotes
# header_valid()
	# 

